#include <stdio.h>
int main()
{
    // int n;
    // printf("Enter a number : ");
    // scanf("%d", &n);

    // if(n % 2 == 0)
    // printf("Even number");
    // else
    // printf("odd number");

    //ternary operator
    // exp1 ? exp2 : exp 3
    //  n % 2 == 0 ? printf("Even number") :  printf("odd number");

    int n = 3;
    if (n = 3)
    printf("%d " , n == 3);
    return 0;
}